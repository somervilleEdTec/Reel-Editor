{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions. The UI talks to the local API over HTTP, so no plugin APIs are exposed.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
