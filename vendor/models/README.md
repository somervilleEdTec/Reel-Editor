# Whisper / faster-whisper model cache
#
# Default installer does not ship large model weights.
# First-run UI can download with user consent into this folder
# (or into the user app-data models directory).
#
# Do not commit model binaries to git.
