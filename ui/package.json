{
  "name": "reelwrite-ui",
  "private": true,
  "version": "2.0.0",
  "description": "Vanilla-JS UI for the local FastAPI. The Tauri 2 shell in src-tauri/ starts the API and loads this UI from http://127.0.0.1:8765/; the browser launcher is the fallback."
}
